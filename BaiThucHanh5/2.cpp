#include <stdio.h>

int main ()
{
	printf ("write a function to calculate area of a triangle. Hint : formula for triangle area : S= bottom_edge*height/2 or S = can bac 2 cua (p*(p-a)*(p-b)*(p-c) with p=(a+b+c)\2. a,c,b are 3 slides off triangle)");
printf ("the function should return -1 for invalid triangle");
	return 0 ;
}


